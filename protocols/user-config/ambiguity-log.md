# Ambiguity log — user-config
## Resolved assumptions
- Closed key set (10 keys) sufficient for v1; reference has a much larger set. [orchestrator]
- Defaults: mode=time, mode2=30, language=english, punctuation=false, numbers=false, difficulty=normal, blindMode=false, stopOnError=off, theme=serika_dark, lazyMode=false. [assumption]
## Open questions
- Whether presets need their own protocol (deferred, out of v1 scope).
