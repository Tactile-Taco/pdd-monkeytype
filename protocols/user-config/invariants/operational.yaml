invariants:
  - id: O-CFG-001
    statement: >
      No outbound network; at most one store write per PUT.
    severity: must
    rationale: >
      Protocol requirement.
    validation:
      - egress-monitor
  - id: O-CFG-002
    statement: >
      GET p95 latency <= 50ms.
    severity: must
    rationale: >
      Protocol requirement.
    validation:
      - resource-budget
