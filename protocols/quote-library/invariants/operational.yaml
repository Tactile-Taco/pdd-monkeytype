invariants:
  - id: O-QT-001
    statement: >
      No outbound network; at most one store write per mutating request.
    severity: must
    rationale: >
      Protocol requirement.
    validation:
      - egress-monitor
  - id: O-QT-002
    statement: >
      Random fetch p95 latency <= 50ms.
    severity: must
    rationale: >
      Protocol requirement.
    validation:
      - resource-budget
