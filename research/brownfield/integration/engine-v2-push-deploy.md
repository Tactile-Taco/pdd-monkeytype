# Integration report — engine-v2 corpus push + pdd-monkeytype-ui deploy

Date: 2026-07-20 (round 03 integration). Executor: integration engineer (subagent).
Source tree: local git `/mnt/agents/work/pdd-monkeytype` @ fe348b2 (clean, committed).
Remote had diverged (MCP/API commits never pulled locally); push performed via
fresh anonymous clone + rsync overlay (no `--delete`; remote-only files such as
`.github/workflows/`, `worker/deploy-ci.sh`, `worker/git-proxy.mjs` preserved).

## Mission 1 — corpus push (local fe348b2 → GitHub main)

37 files pushed (~327 KB) in 7 commits. Final remote head: **bfb62338bdda52d3f73e7157579eb8c3e75fa5f0**.

| Commit | Path | Files | Note |
|---|---|---|---|
| 0b5d187 | push_files | 6 | user-config 1.1.1 bundle |
| 3c96eb7 | push_files | 1 | fix 1 transcription slip in ambiguity-log (caught by post-push read-back) |
| 28787c0 | push_files | 1 | package.json (`node --test` script) |
| e34047cda001eaa70e39511f7ab195a3b1fa0c50 | KV relay | 10 | harness validators + engine modules + app.js |
| 6eb1110383952d56e1e6ba842e3fc79f802a23c8 | KV relay | 10 | typing-test-engine 2.0.0 bundle + tests + validate.js |
| bfb62338bdda52d3f73e7157579eb8c3e75fa5f0 | KV relay | 10 | brownfield research + worker/build.mjs + the two >40 KB files |

Method notes:
- Two files >40 KB (harness/validate-behavioral.mjs 43,175 B; research/paper/pdd-visual-design.md 61,179 B)
  went through the litterbox + pdd-git-proxy KV relay (sha256 fail-closed), per plan.
- After one self-inflicted transcription slip in the first push_files batch, all remaining
  30 files were routed through the hash-gated relay (3 jobs × 10 puts) instead of manual re-emission.
- Post-push verification: 5/5 spot-check files match the clone **exactly by git blob SHA**:
  - implementation/src/engine/session.js — 9460c6d0faf9766496355b54267eee69aaf5ae23
  - harness/validate-behavioral.mjs — 71543a2b30e85bc5970299cf47bf000ef9128e42
  - protocols/typing-test-engine/protocol.yaml — cedb144ebc74f1405afe8766b1913c358e844c61
  - research/metrics/o-ui-005-ci-diagnosis.md — 3ad4ed5e4485fd4f85b87dc2e565772e39aa9eb0
  - implementation/tests/engine-v2.test.mjs — 15fb8b0334a7b1ea59b224047182ebeeb3e45fa3

## CI (pdd-validator-loop workflow, polled via proxy ghGet)

- **Run 29769304228 — head_sha bfb62338 (final push) — conclusion: success.** [2026-07-20T18:48Z]
- Run 29768861103 — 6eb1110 — success.
- Run 29768234746 — e34047c — **failure (expected intermediate state)**: at that commit the
  new harness/engine had landed but the typing-test-engine 2.0.0 protocol bundle had not yet
  (relay job 2). The corpus is only self-consistent at the final SHA; both subsequent runs are green.
  Failed-step detail (ghGet jobs API): job 88439599601 "validate", failed step
  "validator loop (3-layer + ui-presentation)" — the loop ran against a tree still missing the
  typing-test-engine 2.0.0 protocol bundle; ordering artifact, not a regression.
- Runs for 0b5d187, 3c96eb7, 28787c0 — success (protocol/config-only commits validate stand-alone).

## Mission 2 — deploy pdd-monkeytype-ui (engine-v2 candidate)

KV-staged hash-gated upload per docs/09-cloudflare-deployment.md:

- Bundle: worker/bundle.mjs, **104,357 bytes**, sha256 **ba905de188da5cbb3defc4eaa465c7ca6f264df6af3e54717bdc06e72176d889**
  (regenerated by worker/build.mjs with engine-v2 modules; contains quickRestart ×3, wordlist ×19).
- Staging: gzip 32,154 B → base64 42,872 chars → 18 KV chunks `stage:ui:{i}` in namespace
  ef988c65a91c43c680f4ad1e440dd347, written via KV bulk endpoint (6 calls × 3 chunks).
- Hash gate: per-chunk sha256 read-back caught **one 1-char transcription substitution in chunk 16**
  (same failure class as the docs/09 postmortem); re-uploaded and re-verified. All 18 chunks then
  passed; assembled bundle length + sha256 re-verified in-sandbox before upload (fail-closed).
- Upload: PUT workers/scripts/**pdd-monkeytype-ui**, metadata main_module=bundle.mjs,
  compatibility_date 2025-09-01, compatibility_flags [nodejs_compat],
  bindings [PDD_STORE → ef988c65a91c43c680f4ad1e440dd347]. Result: 200 success,
  etag 85a0c14f1a724c540e6f83745472d112. Subdomain: enabled (preexisting).

## Live probes (host has no workers.dev egress; sandbox fetch blocked → verified via browser tool)

- GET https://pdd-monkeytype-ui.pdd-typing.workers.dev/ → **200**, full app renders
  (word stream, mode bar incl. custom, "tab restarts" hint).
- GET /api/quotes/random → **200**, valid quote JSON.
- GET /app.js → served copy contains engine-v2 wiring: `quickRestart` (LOCAL_DEFAULTS
  `quickRestart: "tab"` + Tab/Esc/Enter restart routing), `internalWordlist` (S-ENG-004),
  confidenceMode/freedomMode/strictSpace/oppositeShift/minWpm/minAcc handling (B-ENG-008/010).

## Left clean

- relay:job consumed by cron after each job; staging keys `stage:ui:*` left in KV (harmless; reusable for redeploy of identical bundle).
- Local repo unchanged except this report (untracked); remote-only files untouched.
